sources:
  - name: detection
    description: Raw Fire Detection.
    database: "{{ env_var('PROJECT_ID') }}"
    dataset: "{{ env_var('BQ_DATASET_RAW') }}"
    tables:
      - name: raw_detections
        description: Raw Fire Detection.
      - name: raw_provinces
        description: Raw Provinces.
      - name: raw_regencies
        description: Raw Regencies.